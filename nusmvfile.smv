MODULE main
VAR
	give_med: boolean;
	wlocA: boolean;
	slocA: boolean;
	take_med: boolean;
	get_med: boolean;

LTLSPEC !((G(!get_med) | F((get_med) & F (((F(wlocA & F(slocA)) & (!slocA U wlocA)) & G((wlocA) -> (take_med)) & G((slocA) -> (give_med))))))))
