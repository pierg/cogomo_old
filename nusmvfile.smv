MODULE main
VAR
	low_battery: boolean;
	slocA: boolean;
	contact_station: boolean;
	charge_station: boolean;
	wlocA: boolean;
	get_med: boolean;

LTLSPEC !((((F(charge_station) & G((charge_station))) & G((low_battery) -> X(contact_station))) & (F(slocA) & G((slocA)))))
